import { type Metadata } from "next";

export const metadata: Metadata = {
  title: "Learn more",
  description: "Learn more about the ambassador program",
};

export default function Page() {
  return (
    <div>
      <h1>Learn more about the ambassador program</h1>
    </div>
  );
}
