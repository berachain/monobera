# usePollAllowance

React hook for poll the allowance of a given token

## Import

```ts
import { usePollAllowance } from "@bera/berajs";
```

## Usage

```ts twoslash [example.ts]
import { usePollAllowance, type Token } from "@bera/berajs";

const exampleToken: Token = {
  address: "0x0",
  decimals: 18,
  symbol: "TKN",
  name: "TKN",
}
const { data: allowance, refetch } = usePollAllowance({
  config: {},
  args: {
    spender: "0x0...",
    token: exampleToken,
  },
});

```

## Parameters

```ts
import { type UsePollAllowanceRequest } from "@bera/berajs";
```

## Returns

```ts
import { type UsePollAllowanceResponse } from "@bera/berajs";
```
