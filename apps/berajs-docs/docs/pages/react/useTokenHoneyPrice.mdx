# useTokenHoneyPrice

React hook to retrieve the current honey price of a given token from the Bex subgraph.

## Import

```ts twoslash
import { useTokenHoneyPrice } from "@bera/berajs";
```

## Usage

:::code-group

```ts twoslash [example.ts]
import { useTokenHoneyPrice } from "@bera/berajs";

import { beraConfig } from "./config";

const { data: tokenPriceInHoney } = useTokenHoneyPrice({
  config: beraConfig,
  args: { tokenAddress: "0x0..." },
});
```

```ts twoslash [config.ts] filename="config.ts"
import { BeraConfig } from "@bera/berajs";

// required configs for getTokenHoneyPrice
export const beraConfig: BeraConfig = {
  subgraphs: {
    dexSubgraph: "https://...",
  },
};
```

:::

## Parameters

```ts twoslash
import { type UseTokenHoneyPriceRequest } from "@bera/berajs";
```

## Returns

```ts twoslash
import { type UseTokenHoneyPriceResponse } from "@bera/berajs";
```
