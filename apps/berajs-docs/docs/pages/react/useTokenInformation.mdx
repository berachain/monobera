# useTokenInformation

React hook for grabbing token information for a given address.

## Import

```ts
import { useTokenInformation } from "@bera/berajs";
```

## Usage

:::code-group

```ts twoslash [example.ts]
import { useTokenInformation } from "@bera/berajs";

import { beraConfig } from "./config";

const {
  data: tokenInformation
} = useTokenInformation({
  config: beraJsConfig,
  args: {
    address: "0x0...",
  },
});
```

```ts twoslash [config.ts] filename="config.ts"
import { BeraConfig } from "@bera/berajs";

// required configs for getRoute
export const beraConfig: BeraConfig = {
  contracts: {
    multicallAddress: "0x1..",
  },
};
```

:::

## Parameters

```ts twoslash
import { type UseTokenInformation } from "@bera/berajs";
```

## Returns

```ts
import { type DefaultHookReturnType<Token | undefined> } from "@bera/berajs";
```
