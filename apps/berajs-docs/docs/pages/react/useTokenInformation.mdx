# useTokenInformation

React hook for grabbing token information for a given address.

## Import

```ts
import { useTokenInformation } from "@bera/berajs";
```

## Usage

```ts twoslash [example.ts]
import { useTokenInformation } from "@bera/berajs";
import { BeraConfig } from "@bera/berajs";

// required configs for useTokenInformation
export const beraConfig: BeraConfig = {
  contracts: {
    multicallAddress: "0x1..",
  },
};

const {
  data: tokenInformation
} = useTokenInformation({
    address: "0x0...",
}, {
    beraConfigOverride: beraConfig // optional arg to override default beraConfig
});
```

## Parameters

```ts twoslash
import { type useTokenInformationArgs } from "@bera/berajs";
```

## Returns

```ts twoslash
import { type UseTokenInformationResponse } from "@bera/berajs";
```
