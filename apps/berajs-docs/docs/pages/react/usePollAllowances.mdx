# usePollAllowances

React hook for polling the allowances of a given token array

## Import

```ts
import { usePollAllowances } from "@bera/berajs";
```

## Usage

:::code-group

```ts twoslash [example.ts] filename="example.ts"
import { usePollAllowances, type Token } from "@bera/berajs";

import { beraConfig } from "./config";

const token1: Token = {
  address: "0x0",
  decimals: 18,
  symbol: "TKN",
  name: "TKN",
};

const token2: Token = {
  address: "0x0",
  decimals: 18,
  symbol: "TKN2",
  name: "TKN2",
};

const exampleTokens: Token[] = [token1, token2];

const {
  data: allowances,
  useSelectedAllowance,
  refetch,
} = usePollAllowance({
  config: beraConfig,
  args: {
    spender: "0x0...",
    tokens: exampleTokens,
  },
});

const selectedAllowance = useSelectedAllowance(token1.address);

```

```ts twoslash [config.ts] filename="config.ts"
import { type BeraConfig } from "@bera/berajs";

// required configs for usePollAllowances
export const beraConfig: BeraConfig = {
  contracts: {
    multicallAddress: "0x0...",
  },
};
```

## Parameters

```ts twoslash
import { type UsePollAllowancesRequest } from "@bera/berajs";
```

## Returns

```ts twoslash
import { type UsePollAllowancesResponse } from "@bera/berajs";
```
