# getTokenInformation

Action for fetching token information

## Initialization

Ensure you have followed the steps from [Actions > Getting Started](/actions/getting-started) before getting started

## Import

```ts
import { getTokenInformation } from "@bera/berajs/actions";
```

## Usage

:::code-group

```ts [example.ts]
import { getTokenInformation } from "@bera/berajs/actions";
import { createPublicClient, http } from "viem";
import { berachain } from "viem/chains";

// first, intialize a public client with berachain
const publicClient = createPublicClient({
  chain: berachain,
  transport: http(),
});

// required configs for getWalletBalances
export const beraConfig: BeraConfig = {
    contracts: {
        multicallAddress: '0x...',
    }
}

await getTokenInformation({
  address,
  config: beraConfig,
  publicClient 
});
```

## Parameters

```ts
import { type GetTokenInformation } from "@bera/berajs/actions";
```

## Returns

```ts
import { type GetTokenInformationResponse } from "@bera/berajs/actions";
type result = Promise<GetTokenInformationResponse>;
```