# getTokenInformation

Action for fetching token information

## Import

```ts
import { getTokenInformation } from "@bera/berajs";
```

## Usage

```ts twoslash [example.ts]
import { getTokenInformation } from "@bera/berajs";

import { beraConfig } from "./config";

await getTokenInformation({
  dispatch,
  address,
  config,
  beraConfig,
  setTokenInformation,
  setError,
});
```

```ts twoslash [config.ts] filename="config.ts"
import { BeraConfig } from "@bera/berajs";

// required configs for getSwap
export const beraConfig: BeraConfig = {
  contracts: {
    multicallAddress: "0x...",
  },
};
```

## Parameters

```ts
import { type IFetchTokenPriceInformationArgs } from "berajs";
```
