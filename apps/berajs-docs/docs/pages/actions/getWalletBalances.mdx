# getWalletBalances

Action for fetching the balances of a given wallet address

## Initialization

Ensure you have followed the steps from [Actions > Getting Started](/actions/getting-started) before getting started

## Import

```ts
import { getWalletBalances } from "@bera/berajs";
```

## Usage

```ts
import { getWalletBalances, type Token } from "@bera/berajs";
import { createPublicClient, http } from "viem";
import { berachain } from "viem/chains";

// first, intialize a public client with berachain
const publicClient = createPublicClient({
  chain: berachain,
  transport: http(),
});

const walletBalances = await getWalletBalances({
  account,
  tokenList,
  externalTokenList,
  queryKey,
  publicClient,
  mutate,
});
```

## Parameters

```ts twoslash
import { type IFetchWalletBalancesRequestArgs } from "@bera/berajs";
```

## Returns

```ts
type result = Promise<number | BalanceToken[] | undefined>;
```
